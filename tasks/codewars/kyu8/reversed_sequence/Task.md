# Reversed Sequence (8kyu)

## 📝 Description
Build a function that returns a list of integers from n to 1 where n > 0.

Example:
n = 5 → [5, 4, 3, 2, 1]

## 💡 Idea
Use Kotlin range with `downTo` and convert it to a list using `toList()`.

## ⏱ Complexity
Time: O(n)  
Space: O(n)

## 🧠 Notes
Input is guaranteed to be greater than 0.
